resources:
  jobs:
    mlops_drift_cycle:
      name: mlops-drift-cycle-${bundle.target}
      max_concurrent_runs: 1
      tasks:
        - task_key: run_mlops_drift_cycle
          notebook_task:
            notebook_path: ../../notebooks/free_edition/mlops_drift_cycle.py
            source: WORKSPACE

    mlops_postprod_cycle:
      name: mlops-postprod-cycle-${bundle.target}
      max_concurrent_runs: 1
      tasks:
        - task_key: run_mlops_postprod_cycle
          notebook_task:
            notebook_path: ../../notebooks/free_edition/mlops_postprod_cycle.py
            source: WORKSPACE

    mlops_retraining_cycle:
      name: mlops-retraining-cycle-${bundle.target}
      max_concurrent_runs: 1
      tasks:
        - task_key: run_mlops_retraining_cycle
          notebook_task:
            notebook_path: ../../notebooks/free_edition/mlops_retraining_cycle.py
            source: WORKSPACE

    mlops_operational_cycle:
      name: mlops-operational-cycle-${bundle.target}
      max_concurrent_runs: 1
      tasks:
        - task_key: run_mlops_operational_cycle
          notebook_task:
            notebook_path: ../../notebooks/free_edition/mlops_operational_cycle.py
            source: WORKSPACE
