bundle:
  name: b3-databricks-platform

include:
  - resources/jobs/*.yml

targets:
  dev:
    default: true
    workspace:
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
  hml:
    workspace:
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
  prd:
    workspace:
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
