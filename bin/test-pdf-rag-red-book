#!/usr/bin/env bash
set -euo pipefail

time curl -v --max-time 30 -X POST http://127.0.0.1:8010/chat \
  -H 'Content-Type: application/json' \
  -d '{"question":"No PDF sobre o Livro Vermelho, qual é o tema principal e como ele é introduzido?"}'
echo
