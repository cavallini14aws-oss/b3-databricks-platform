pipeline:
  name: template_ai_pipeline
  domain: template
  layer: ai
  enabled: true

execution:
  mode: batch

retrieval:
  enabled: true
  chunk_size: 1000
  chunk_overlap: 100

evaluation:
  enabled: true
