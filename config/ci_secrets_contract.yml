providers:
  github_actions:
    dev:
      required:
        - DEV_WORKSPACE_HOST
        - DEV_DATABRICKS_TOKEN
        - DEV_CLUSTER_ID
    hml:
      required:
        - HML_WORKSPACE_HOST
        - HML_DATABRICKS_TOKEN
        - HML_CLUSTER_ID
    prd:
      required:
        - PRD_WORKSPACE_HOST
        - PRD_DATABRICKS_TOKEN
        - PRD_CLUSTER_ID

  azure_devops:
    dev:
      required:
        - DEV_WORKSPACE_HOST
        - DEV_DATABRICKS_TOKEN
        - DEV_CLUSTER_ID
    hml:
      required:
        - HML_WORKSPACE_HOST
        - HML_DATABRICKS_TOKEN
        - HML_CLUSTER_ID
    prd:
      required:
        - PRD_WORKSPACE_HOST
        - PRD_DATABRICKS_TOKEN
        - PRD_CLUSTER_ID

  bitbucket:
    dev:
      required:
        - DEV_WORKSPACE_HOST
        - DEV_DATABRICKS_TOKEN
        - DEV_CLUSTER_ID
    hml:
      required:
        - HML_WORKSPACE_HOST
        - HML_DATABRICKS_TOKEN
        - HML_CLUSTER_ID
    prd:
      required:
        - PRD_WORKSPACE_HOST
        - PRD_DATABRICKS_TOKEN
        - PRD_CLUSTER_ID

  aws:
    dev:
      required:
        - DEV_WORKSPACE_HOST
        - DEV_DATABRICKS_TOKEN
        - DEV_CLUSTER_ID
    hml:
      required:
        - HML_WORKSPACE_HOST
        - HML_DATABRICKS_TOKEN
        - HML_CLUSTER_ID
    prd:
      required:
        - PRD_WORKSPACE_HOST
        - PRD_DATABRICKS_TOKEN
        - PRD_CLUSTER_ID
